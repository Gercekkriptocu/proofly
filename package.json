{
  "name": "proofly",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": { "dev": "vite", "build": "npm run compile:contract && vite build", "compile:contract": "node scripts/compile-contract.mjs", "deploy:mainnet": "npm run compile:contract && node scripts/deploy-mainnet.mjs", "preview": "vite preview" },
  "dependencies": { "@vitejs/plugin-react": "latest", "vite": "latest", "react": "latest", "react-dom": "latest", "solc": "latest", "viem": "latest" },
  "devDependencies": {}
}
